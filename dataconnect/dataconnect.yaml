specVersion: "v1beta"
serviceId: "reitha"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "reitha-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
